Wenn du willst, hier ein sinnvoller “Restarbeiten”-Plan (ohne Gelaber):


DB-Constraints/Feldlängen final (eingabe/wertung/frage etc.)

Code-Fix gegen “Enter festgehalten” (serverseitig begrenzen)

Backup (pg_dump) + ggf. Cron

Performance: Indexe für Protokoll (profil_id, start, kategorie_id)

SQLite-Datei & alte Dumps aufräumen

Smoke test: Login/Admin/Übungen/Schreiben ins Protokoll

1. ich glaube, der Seitenaufbau ist langsamer - ich muss mich wohl um indizierung kümmern 

psql -h ~/tmp -U rt -d rechentrainer -c "CREATE INDEX CONCURRENTLY IF NOT EXISTS core_protokoll_profil_id_idx ON core_protokoll (profil_id);"
psql -h ~/tmp -U rt -d rechentrainer -c "CREATE INDEX CONCURRENTLY IF NOT EXISTS core_protokoll_start_idx ON core_protokoll (start);"
psql -h ~/tmp -U rt -d rechentrainer -c "CREATE INDEX CONCURRENTLY IF NOT EXISTS core_protokoll_kategorie_id_idx ON core_protokoll (kategorie_id);"

2. jetzt könnte man auch zwei worker nutzen 

Faustregel: 2–4 Worker auf Uberspace meist okay.
Setz testweise auf 2:
In deiner gunicorn/supervisor config (die Zeile mit gunicorn ...):
add --workers 2
Dann:
supervisorctl restart gunicorn

3. diese "wertung" "frrrrrrrrrrrrrrrrrr" zeigt, dass die Kids wohl eine Möglichkeit gefunden haben, den rechentrainer auszutricksen und für ene Aufgabe mehrere "richtig" Wertungen zu bekommen 

4. das führt dazu, dass wir auch verhindern müssen, dass die einträge in "eingab", "wertung" und "geloescht.text" zu lang werden 
in Protokoll.save():

eingabe = eingabe[:100]
wertung = wertung[:10]
in Geloescht.save():

text = text[:200] (oder was du willst)

5. dann sind noch ein paar fehler im code 

6. dann will ich meinen "physiktrainer" anfangen und dazu 

7. apps und möglichst auch felder umbenennen (mit letzterem gab es aber große probleme) und 

8. brauche ich wohl schon wieder einen neuen chat - du bist schon wieder dauernd abwesend :(